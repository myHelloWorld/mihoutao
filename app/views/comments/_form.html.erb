<%= form_for([@item,@item.comments.build]) do |f| %>
  <%= f.label :commenter %>
  <%= f.text_field :commenter %>

  <div>
    <%= f.label :body %> <br />
    <%= f.text_area :body %>
  </div>
  <%= f.submit %>
<% end %>

